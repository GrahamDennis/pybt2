# This requirements are for development and testing only, not for production.
pytest
pytest-asyncio
aiotools @ git+https://github.com/achimnol/aiotools@main # because the latest release doesn't have types
coverage
pytest-cov
mypy
gitchangelog
mkdocs
ruff
